// Storyteller is a client-rendered SPA against a local API: no SSR, no
// prerender. The static adapter emits a fallback shell (svelte.config.js).
export const ssr = false;
export const prerender = false;
